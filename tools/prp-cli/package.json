{
  "name": "@cortex-os/prp-cli",
  "version": "0.1.0",
  "description": "CLI utilities for Cortex-OS PRP run manifests, policies, and signatures",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "prp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint src/**/*.ts",
    "test": "node ../../scripts/vitest-safe.mjs run -c vitest.config.ts --run --reporter=dot"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "fast-glob": "^3.3.3",
    "table": "^6.8.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "esbuild": "^0.25.0",
    "tslib": "^2.8.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
