[project]
name = "cortex-ml"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["openai>=1.52.0", "instructor>=1.11.2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cortex_ml"]
