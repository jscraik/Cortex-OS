[project]
name = "cortex-core"
version = "0.1.0"
# Align with workspace (grpcio-tools wheel availability)
requires-python = ">=3.11,<3.13"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cortex_core"]
