# Runtime environment variables (developer local baseline)
# This file is intentionally versioned to avoid accidental deletion.
# DO NOT store real secrets; use 1Password/secret manager for production.

# Sonar
SONAR_HOST_URL=https://sonarcloud.io
# Real token should come from GitHub Actions secret SONAR_TOKEN (left blank locally)
SONAR_TOKEN=

# Neo4j
NEO4J_AUTH=neo4j/CHANGE_ME

# Grafana
GF_SECURITY_ADMIN_PASSWORD=CHANGE_ME

# API Keys (leave blank locally or load via env:load)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GITHUB_TOKEN=

# Feature flags
FEATURE_EXPERIMENTAL_AGENTS=false
