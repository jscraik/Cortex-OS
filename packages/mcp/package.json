{
  "name": "@cortex-os/mcp",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "readiness:models": "tsx scripts/check-model-readiness.ts"
  },
  "dependencies": {
    "@cortex-os/contracts-v2": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "tsx": "^4.0.0",
    "vite-tsconfig-paths": "^5.1.4"
  }
}
