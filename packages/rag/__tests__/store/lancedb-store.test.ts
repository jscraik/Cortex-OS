import { describe, it } from 'vitest';

describe('LanceDB store (placeholder)', () => {
	it.skip('requires LanceDB runtime to execute', () => {
		// Placeholder suite - real LanceDB tests execute in integration pipeline
	});
});
