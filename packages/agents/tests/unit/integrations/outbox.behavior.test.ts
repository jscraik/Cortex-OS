/**
 * LEGACY PLACEHOLDER TEST (INTENTIONALLY SKIPPED)
 * Original test file became corrupted with duplicated async fragments.
 * Real coverage lives in:
 *  - outbox.behavior.clean.test.ts
 *  - outbox.size-redaction.test.ts
 *  - outbox.test.ts
 * This stub prevents transform errors while retaining a historical filename.
 */
import { describe, it } from 'vitest';

describe.skip('outbox.behavior (legacy-corrupted placeholder)', () => {
    it('is intentionally skipped', () => {
        // no-op: behavior validated in clean tests
    });
});

export { };

