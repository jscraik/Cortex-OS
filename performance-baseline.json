{
    "schemaVersion": 1,
    "targets": {
        "build": {
            "maxMs": 600000
        },
        "test": {
            "maxMs": 900000
        },
        "lint": {
            "maxMs": 300000
        },
        "typecheck": {
            "maxMs": 600000
        }
    },
    "notes": "Initial baseline. Adjust after first real CI measurements. maxMs acts as soft gate; overshoot triggers non-zero exit unless ALLOW_PERF_DRIFT is set."
}
