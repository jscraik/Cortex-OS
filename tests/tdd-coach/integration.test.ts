import { describe, it } from 'vitest';

// Placeholder test to enforce red state until TDD Coach integration is wired.
describe('TDD Coach Integration', () => {
	it('should block commits until TDD Coach hooks run in watch mode', () => {
		throw new Error('Pending TDD Coach pre-commit integration.');
	});
});
