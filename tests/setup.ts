/**
 * Legacy Vitest setup shim.
 *
 * Some project-specific configurations still reference this module. It is left
 * intentionally minimal to avoid failing test discovery while we migrate to
 * scoped setup files.
 */

export {};
