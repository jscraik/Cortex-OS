#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

pnpm lint-staged
pnpm typecheck
pnpm run validate:contracts
pnpm run cortex:validate
if command -v uv >/dev/null 2>&1; then
  (cd python && uv run ruff check . && uv run pyright) || exit 1
fi
