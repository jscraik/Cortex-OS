# Central port registry for local services (Cloudflare tunnel aware)
# Edit these to change the bound ports; scripts will source this file.

# Reserved by Cloudflare tunnel mapping
MCP_PORT=3000
GITHUB_AI_PORT=3001
SEMGREP_PORT=3002
STRUCTURE_PORT=3003

# Documentation site
DOCS_PORT=3005

# Local Memory service (default base URL http://localhost:3010/api/v1)
# Moved from 3002->3004->3010 to avoid conflicts with Cloudflare tunnel reserved ports (3000-3003)
LOCAL_MEMORY_PORT=3010

# ASBR (AI System Blueprint Refactor) API Server
ASBR_PORT=7439

# Future allocations (reserved to avoid collision)
# MARKETPLACE_API_PORT=3011
# CORTEX_WEBUI_PORT=3012
