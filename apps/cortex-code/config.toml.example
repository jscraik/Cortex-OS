# DEPRECATED: Use JSON config instead of TOML
# This file remains only for legacy installs and will be removed in a future release.
# Create and edit: ~/.cortex/cortex.json
# See docs: apps/cortex-code/docs/cortex-code-docs/user-guide.md (Configuration)

# Example JSON (for reference only):
```json
{
  "providers": { "default": "github-models" },
  "server": { "cloudflare": { "tunnel_name": "cortex-dev", "auto_start": false } },
  "webui": { "enabled": true, "port": 3000 }
}
```
