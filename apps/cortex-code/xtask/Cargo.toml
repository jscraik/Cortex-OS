[package]
name = "codex-xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.16", features = ["derive"] }
anyhow = "1.0"
which = "6.0"
indicatif = "0.17"

[package.metadata]
description = "Developer automation tasks for cortex-code (coverage, maintenance, etc.)"
