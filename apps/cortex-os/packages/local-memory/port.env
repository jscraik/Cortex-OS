# Local Memory REST API Server Configuration
# Synced with config/ports.env (canonical source)
# Last updated: 2025-10-11
#
# IMPORTANT: This is the OPTIONAL REST API server configuration.
# For MCP protocol access (recommended), use the MCP Server at port 3024.
# See packages/mcp-server/README.md

# REST API Server Configuration
MEMORY_API_PORT=3028                    # REST API port
LOCAL_MEMORY_PORT=3028                  # Backward compatibility alias
LOCAL_MEMORY_HOST=127.0.0.1             # Bind address
LOCAL_MEMORY_BASE_URL=http://127.0.0.1:3028  # Base URL

# Logging
MEMORY_LOG_LEVEL=info

# Development flags
NO_AUTH=false  # Set to true for development without authentication

# Note: MCP Server runs separately on port 3024 (packages/mcp-server)
# Both services use the same memory-core backend
