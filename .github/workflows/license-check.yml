name: License Compliance (LEGACY - Consolidated)

on:
  # Disabled, license validation now part of unified-security & CI governance.
  workflow_dispatch:

jobs:
  license:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - run: pnpm install --frozen-lockfile
      - name: Check licenses
        run: pnpm deps:license
